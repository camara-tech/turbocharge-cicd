{
    "runOnSave.statusMessageTimeout": 3000,
    "runOnSave.commands": [
        {
            "globMatch": "*",
            "cmd": "commit.sh ${file}",
            "runIn": "terminal"
        },
    ]
}